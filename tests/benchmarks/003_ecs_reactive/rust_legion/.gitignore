# cargo build artifacts — regenerated by run_legion_anchor.sh.
# target-soa/ is the `--features soa_grid` layout control, built with a separate
# target dir so the two binaries can be measured side by side.
/target/
/target-soa/
