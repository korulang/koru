A call inside an if CONDITION expression is illegal — calls are not
expressions; the idiom is bind-first (`g(a: 4): x |> if(x > 3)`).

INTENTIONAL PIN — currently RED as wrong-error: the compiler accepts the
shape and leaks the call's Koru syntax verbatim into emitted Zig (raw
Stage-D host error) instead of rejecting at the koru level. One of the
five open surfaces mapped 2026-07-05 (arg position 320_127, if-condition,
produce RHS, captured field, for-bound); the guarded surfaces are branch
constructors (PARSE003) and std/io interpolations. Goes green when the
single expression-admission wall lands.
