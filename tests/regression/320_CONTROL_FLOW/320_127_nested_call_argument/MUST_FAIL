A call nested directly in argument position — r1(x: r1(x: 5)) — is ILLEGAL
(Lars-ruled 2026-07-05). The idiom is bind-first: `r1(x: 5): a |> r1(x: a)`.

INTENTIONAL PIN — currently RED as wrong-error. The compiler accepts the
construct through Stage A/B and leaks the inner call's Koru syntax verbatim
into emitted Zig (raw Stage-D host error). The fix is a koru-level rejection
wall with a bind-the-inner-result-first hint; the exact diagnostic code is
open until the wall is built. When it lands, this goes green.
