Allow standalone // comments inside flow continuations

Currently the parser errors on:
```koru
~step1()
| done |>
    // This comment breaks parsing
    step2()
```

Fix: Parser should discard lines matching `^\s*//` inside flow continuation blocks.

We likely already handle // as EOL comments in other contexts - extend that to flows.
