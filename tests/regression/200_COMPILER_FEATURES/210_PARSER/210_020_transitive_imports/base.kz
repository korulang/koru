// Base module - lowest level in transitive import chain
// This file should appear in AST even though input.kz doesn't directly import it

const std = @import("std");

~pub tor double { x: i32 } -> i32

~proc double|zig {
    const result = x * 2;
    return result;
}
