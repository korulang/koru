// JavaScript implementation facet. The events are declared in input.kz
// (merged at import); this file holds the |js proc bodies.

~proc compute|js {
    return x * 2;
}

~proc run-test|js {
    // This internally tests braceless continuation constructors
}
