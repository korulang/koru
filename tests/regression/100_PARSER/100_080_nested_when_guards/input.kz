// TEST: Nested continuations with when-guards should parse without parse-error nodes.

~event run {} -> i32

~event poll { t: i32 } -> u8

~event cleanup {}

~event handle-key { code: u8 }

~run(): t |> poll(t.t)
    | key k when k.code == 'q' |> cleanup()
    | key k |> handle-key(k.code)
