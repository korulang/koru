// Base module - lowest level in import chain
// Defines a simple pure operation

const std = @import("std");

~pub event double { x: i32 } -> i32

~proc double|zig {
    const result = x * 2;
    return result;
}
