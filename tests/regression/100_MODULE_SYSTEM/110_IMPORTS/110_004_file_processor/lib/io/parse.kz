// Parse module

const std = @import("std");

~pub tor lines { text: string } -> []string

~proc lines|zig {
    std.debug.print("parse lines ({} chars)\n", .{text.len});
    const empty: []string = &.{};
    return .{ .@"parsed" = empty };
}
