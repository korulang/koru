// Parse module

const std = @import("std");

~pub event lines { text: string } -> [][]const u8

~proc lines|zig {
    std.debug.print("parse lines ({} chars)\n", .{text.len});
    const empty: [][]const u8 = &.{};
    return .{ .@"parsed" = empty };
}
