// Audio module for raylib

const std = @import("std");

~pub event play { sound_id: i32 }
| playing

~pub event stop {}
| stopped

~proc play {
    return .{ .@"playing" = .{} };
}

~proc stop {
    return .{ .@"stopped" = .{} };
}
