// Audio subsystem

const std = @import("std");

~pub event init {}

~pub event shutdown {}

~proc init|zig {
    std.debug.print("Audio initialized\n", .{});
}

~proc shutdown|zig {
    std.debug.print("Audio shutdown\n", .{});
}
