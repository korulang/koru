// Audio subsystem

const std = @import("std");

~pub tor init {}

~pub tor shutdown {}

~proc init|zig {
    std.debug.print("Audio initialized\n", .{});
}

~proc shutdown|zig {
    std.debug.print("Audio shutdown\n", .{});
}
