// Physics subsystem

const std = @import("std");

~pub event init {}

~pub event shutdown {}

~proc init|zig {
    std.debug.print("Physics initialized\n", .{});
}

~proc shutdown|zig {
    std.debug.print("Physics shutdown\n", .{});
}
