// Physics subsystem

const std = @import("std");

~pub tor init {}

~pub tor shutdown {}

~proc init|zig {
    std.debug.print("Physics initialized\n", .{});
}

~proc shutdown|zig {
    std.debug.print("Physics shutdown\n", .{});
}
