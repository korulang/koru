// Physics subsystem

const std = @import("std");

~pub event init {}

~pub event shutdown {}

~proc init {
    std.debug.print("Physics initialized\n", .{});
}

~proc shutdown {
    std.debug.print("Physics shutdown\n", .{});
}
