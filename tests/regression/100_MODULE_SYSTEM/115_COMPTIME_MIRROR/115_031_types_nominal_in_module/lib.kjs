// JavaScript implementation facet. The events are declared in lib.kz (merged
// at import); this file holds the |js proc bodies.

~proc greet-user|js {
    console.log(`Hello ${name} (ID: ${id})`);
}
