let counter = 0;

~proc run|js { for (let i = 0; i < n; i++) { if (i % 2 === 0) { key(i); } else { tick(i); } } }
~proc bumpKey|js { counter = counter + v; }
~proc bumpTick|js { counter = counter + v * 10; }
~proc report|js { console.log("counter = " + counter); }
