const std = @import("std");
var count: u64 = 0;

~proc run|zig {
    var i: u64 = 0;
    while (i < n) : (i += 1) {
        const c: u64 = 42;
        key(c);
    }
}

~proc onKey|zig { count = count + 1; }
~proc report|zig { std.debug.print("count={d}\n", .{count}); }
