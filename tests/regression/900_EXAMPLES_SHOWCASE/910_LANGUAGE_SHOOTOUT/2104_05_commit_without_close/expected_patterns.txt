# Test 2104_05: Commit without close — uncleaned Connection[active!]
#
# INTENT: After `commit`, the connection returns in [active!] state
# requiring `close`. The test omits `close` — phantom checker should
# reject this with a clear KORU diagnostic about the undischarged
# `Connection` obligation, pointing at the user's source.
#
# CURRENTLY: Backend codegen emits invalid Zig (`function with
# non-void return type ... implicitly returns`) — a codegen bug in
# the discharge-event path lets this through the frontend. Real
# compiler bug — pin captures INTENT, test stays red as wrong-error
# until codegen for void-discharge events is fixed and the frontend
# catches the missing `close`.
error\[KORU\d+\]
Connection
not discharged
input\.kz
