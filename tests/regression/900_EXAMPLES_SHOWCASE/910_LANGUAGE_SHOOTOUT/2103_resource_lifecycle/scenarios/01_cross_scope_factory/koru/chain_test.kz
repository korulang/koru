// Test: Escape + one borrow + cleanup

const std = @import("std");

const File = struct { handle: i32 };

~tor open { path: string }
| opened *File<opened!>

~proc open|zig {
    std.debug.print("Opening\n", .{});
    const f = std.heap.page_allocator.create(File) catch unreachable;
    f.* = File{ .handle = 42 };
    return .{ .opened = f };
}

~tor use { file: *File<opened> }

~proc use|zig {
    std.debug.print("Using\n", .{});
}

~tor close { file: *File<!opened> }

~proc close|zig {
    std.debug.print("Closing\n", .{});
    std.heap.page_allocator.destroy(file);
}

// Factory that escapes obligation
~tor create { path: string }
| created *File<opened!>

~create = open(path)
| opened f => created { resource: f.file }

// Main flow: create -> use -> cleanup
~create(path: "test.txt")
| created r |> use(file: r.resource) |> close(file: r.resource)

pub fn main() void {}
