# Examples Showcase

Working examples that appear on korulang.org. These tests ensure the website never shows broken code.

## 900_HELLO_WORLD

The front page "Hello World" example demonstrating:
- Liquid-style templating with `{{ var }}`
- Runtime conditionals with `{% if %}`
- Multi-line block syntax with `print.blk`
- Type-safe format specifiers

This exact code appears on the korulang.org homepage.
