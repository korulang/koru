IDEA PIN (queue (k), x3 gauntlet convergence): the disposal verb — a taken
row's synthesized obligation is discharged by `give-back(item)` (spelling
floated by two independent agents; corpus discharge idiom = void call in
consume position, std/list:free / Connection:close). Unblocks every
delete flow (todo delete, ORM evict, arena death; 690_007's sibling).
