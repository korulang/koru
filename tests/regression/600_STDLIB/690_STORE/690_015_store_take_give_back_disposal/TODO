IDEA PIN — DESIGN-GATED, not machinery-gated (2026-07-05 sweep): the
`give-back` disposal verb is ruling-queue item (k), an OPEN Lars ruling.
take + the <store-item!> obligation are BUILT (690_007 green rejects the
leak); the legal sibling cannot exist until (k) names the discharge verb
and its spelling. Do NOT invent it. Green when (k) is ruled + built.
