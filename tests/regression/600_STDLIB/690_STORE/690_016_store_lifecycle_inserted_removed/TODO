IDEA PIN (ruling 5, rung two) — BLOCKED on TWO opens (2026-07-05
sweep): (1) the (k) disposal verb — this pin's flow ends `take |>
give-back`, undesigned (see 690_015); (2) plural lifecycle payload
vocabulary (gauntlet-2 unification: what `inserted`/`removed` bind on a
plural store). Interceptors-on-plural machinery itself is a loud wall in
store.kz pointing here. Green when (k) + the vocabulary are ruled.
