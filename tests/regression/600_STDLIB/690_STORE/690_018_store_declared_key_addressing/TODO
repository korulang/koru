IDEA PIN (O2 head 3) — WALLED on infra (2026-07-05 sweep): key
addressing as an LVALUE (stored target) is buildable today, but the pin
also uses `pool[id: 7].hp` as an RVALUE inside a print interpolation in
ORDINARY user flow text — rewriting that needs either whole-program
rewrite capability for store transforms (taps-style whole_program
rebuild; SiteResult is replacement+appended only) or expression-layer
lowering of key paths. Infrastructure/design fork — float to Lars.
Lvalue-only subset could pin separately if wanted.
