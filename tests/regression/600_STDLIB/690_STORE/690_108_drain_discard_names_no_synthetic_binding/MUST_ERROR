A refusal names surface the author wrote.

`! discharge _` discards the element the arm exists to drain. Today that reaches
the obligation checker and comes back as a complaint about `_auto_N` — a binding
minted by the compiler, carrying a serial that moves with the shape of the
program, appearing nowhere in the source. The author's only lever is the `_` they
typed. The pin matches the prefix for that reason.

RULED (Lars, 2026-07-29): the discard is refused, and not as an unused-binding
violation. A drain arm exists BECAUSE the column has no drain path the store can
insert on its own — if one existed the arm would not be needed. So discarding the
element is the one thing the construct cannot mean. `! inserted` teaches `_` as
the escape (690_032); a drain arm is the one place that escape has nothing to
escape from.

PIN: the refusal names the construct and says why the discard is empty, and no
`_auto_N` reaches the author.

The bare form is the one that drops a name legitimately — 690_109 pins
`! discharge |> ...()`, where the column threads into the drain call and nothing
is discarded at all.
