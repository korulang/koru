# Test 520_001: Slice types from imported modules
#
# INTENT (from test header): Demonstrate the codegen bug where an
# event returning `[]const ImportedType` emits invalid Zig like
# `koru_[]const mod.Type` instead of `[]const koru_mod.Type`.
# Discovered while building $std/threading.
#
# CURRENTLY: Test fails earlier — `| got |> _` has a payload-bearing
# branch but no binding (`| got handles |> _` would be required to
# reach the codegen path that exhibits the original bug). Pin
# captures current behavior so the test isn't a silent pass; the
# test itself needs rewriting to bind the payload before the
# original codegen issue can be re-exercised.
error\[KORU\d+\]
branch 'got'
no binding
