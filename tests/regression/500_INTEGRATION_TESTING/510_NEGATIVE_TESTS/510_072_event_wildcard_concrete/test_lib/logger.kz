// Logger for event wildcard tap test

const std = @import("std");
~import std/taps

~pub tor log { msg: string }

~proc log|zig {
    std.debug.print("[TAP] {s}\n", .{msg});
}

// Event wildcard tap using metatype (valid usage)
~tap(*:log -> *)
| Profile p |> log(msg: p.source)
