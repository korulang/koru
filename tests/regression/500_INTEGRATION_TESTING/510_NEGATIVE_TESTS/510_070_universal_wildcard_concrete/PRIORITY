Fix metatype binding codegen in tap transform - WILDCARD PATTERN TEST

DESCRIPTION:
When wildcard taps use metatype branches (Profile), the generated code fails
to properly bind the metatype variable (e.g., 'p').
Error: "use of undeclared identifier 'p'" when accessing p.source.

This test CONFIRMS wildcard pattern matching is working (wildcards ARE matching),
but metatype binding is broken (variable scope is lost).

ACCEPTANCE CRITERIA:
1. Wildcard patterns match (taps ARE inserted - VERIFIED ✓)
2. Profile variable 'p' accessible in tap handler invocation
3. Test passes with output: "[TAP] input:hello\nHello\n[TAP] input:goodbye\nGoodbye\n"

RELATED / DEPENDS ON:
- 310_041_metatype_profile_binding (PRIORITY) - Profile metatype binding
- 310_042_metatype_transition_binding (PRIORITY) - Transition metatype binding
- 310_043_metatype_audit_binding (PRIORITY) - Audit metatype binding

These three metatype tests are SIMPLER and isolate the root cause.
Fix those first, then this wildcard test will pass.

ALSO BLOCKS:
- 310_018 (CCP opt-in) and 310_019 (CCP flag injection)
