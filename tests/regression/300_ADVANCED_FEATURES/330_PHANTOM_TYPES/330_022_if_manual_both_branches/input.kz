~import "$std/control"
~import "$app/fs"
const condition = true;
~app.fs:open(path: "test.txt")
| opened f |>
    std.control:if(condition)
    | then |>
        app.fs:close(file: f)
    | else |>
        app.fs:close(file: f)
pub fn main() void {}
