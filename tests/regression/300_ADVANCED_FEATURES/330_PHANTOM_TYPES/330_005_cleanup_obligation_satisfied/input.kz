// Test 514: Cleanup obligation satisfied
// Tests that phantom checker accepts proper cleanup
// Demonstrates: ! marker properly handled when resource is cleaned up

~import "$app/fs"

// Open a file with opened! state and properly close it
~app.fs:open(path: "test.txt")
| opened f |> app.fs:close(file: f)
    | closed |> _
