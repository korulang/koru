~import "$app/db"
const std = @import("std");
~app.db:connect(host: "localhost")
| connected _ |> app.db:begin(conn: _.conn)
    | begun _ |> app.db:do_work(tx: _.tx)
pub fn main() void {
    std.debug.print("Cascading auto-discharge test\n", .{});
}
