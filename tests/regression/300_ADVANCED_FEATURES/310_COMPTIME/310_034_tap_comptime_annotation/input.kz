// Test for tap with [comptime] annotation
// This currently fails due to a type system issue with annotation serialization

~[comptime]

const std = @import("std");

~event start {}
| done {}

~event end {}

~event log {}

~[comptime]proc log {
    std.debug.print("log\n", .{});
}

// Tap with comptime annotation - should work but currently fails
~[comptime]start -> end
| Profile |> end()
