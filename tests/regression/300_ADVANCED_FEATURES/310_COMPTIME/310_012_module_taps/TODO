Tap transformer index out of bounds when inserting cross-module taps

The circular import issue is FIXED (cycle detection added to ModuleResolver).

Current error in tap_transformer.zig:599 - insertTapInvocations():
  index out of bounds: index 2, len 2

The tap is being found and matched correctly:
  TAP REGISTRY: Registered tap: source='input:main_event' dest='(null)' branch='done'
  TAP TRANSFORMER: Checking taps FROM 'input:main_event' ON 'done' → 1 matches
  TAP TRANSFORMER: Inserting 1 tap(s) into continuation pipeline!

But the insertion logic has a bug with pipeline sizing/indexing.
