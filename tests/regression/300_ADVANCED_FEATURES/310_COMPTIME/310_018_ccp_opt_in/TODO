BLOCKED: Waiting for opaque annotation support in tap transform

This test is blocked by:
  310_040_opaque_tap_skipping (PRIORITY)

The emit_transition event is marked [opaque] to prevent infinite recursion,
but the tap transform doesn't yet check for [opaque] annotations.

When 310_040 is fixed (implement opaque checking in taps.kz), this test
will automatically be able to run.
