// TEST: kernel.pairwise basic iteration
//
// STATUS: BROKEN - kernel continuation handling needs rework
//
// The issue:
// 1. kernel:init creates a nop event with no branches
// 2. The continuation | kernel k |> expects a kernel branch
// 3. Mismatch = error
//
// FIX NEEDED:
// - kernel:init should emit code at module scope and flatten continuations
// - OR make nop event have a kernel branch with proper shape

~import "$std/kernel"
~import "$std/io"

// This is what we WANT to write:
//
// ~std.kernel:init(Body) {
//     { x: 0.0, y: 0.0, mass: 1.0 },
//     { x: 1.0, y: 0.0, mass: 2.0 },
// } | kernel k |> std.kernel:pairwise { k.mass += k.other.mass }
//     | done |> print results
//
// But continuation handling is broken

~std.io:print.ln("kernel pairwise test needs rework")
