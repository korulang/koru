// codemod:skip — negative test: must keep its `println` call to exercise removal.
// PIN: `std/io:println` is DELETED (subsumed by `print.ln`). Calling the removed
// event fails loudly — KORU040 "unknown event" at compiler coordination (Stage C).
// Greenfield: the old form must fail once removed.
import std/io

~std/io:println("println should be gone")
