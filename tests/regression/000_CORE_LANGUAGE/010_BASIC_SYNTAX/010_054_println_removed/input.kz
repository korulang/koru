// DEFERRED PIN: `std/io:println` removal (#3). Blocked on the `std/compiler`
// stage facet: the correct replacement is `std/compiler:print.ln` (a runtime-
// safe, NON-transform print), NOT `std/io:print.ln` (a comptime transform that
// can't serve as a runtime event — subflow bodies, taps, cross-target JS, and
// runtime-eval all need the runtime form). println and print.ln are different
// and both currently necessary. RED until the std/compiler facet lands and
// println moves there. EXPECT stays FRONTEND_COMPILE_ERROR (the target state).
import std/io

~std.io:println(text: "println should be gone once std/compiler:print.ln exists")
