// Test: print.ln bare expression shorthand
//
// std.io:print.ln(expr:fmt) works without "{{ }}" wrapping.
// A non-quoted argument is a bare expression, not a string literal.

~import "$std/io"

~event compute {}
|> std.io:print.ln(r.count:d)
    |> std.io:print.ln(r.label:s)
