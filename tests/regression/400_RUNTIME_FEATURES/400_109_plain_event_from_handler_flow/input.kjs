// JavaScript implementation facet. Events are declared in input.kz (merged at
// import); this file holds the |js proc bodies.

~proc emit-one|js {
    step(7);
    return { tag: "done", done: 35 };
}

~proc plain|js {
    console.log(`plain = ${v}`);
}
