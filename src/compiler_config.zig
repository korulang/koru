// Koru Compiler Configuration
// Feature flags for toggling experimental features and comparing implementations

// Note: AST tap transformation is now the only system - emission-time tap injection removed
