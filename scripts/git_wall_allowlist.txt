# Paths the committed tree may carry even though .gitignore would refuse them fresh.
# One path prefix or exact path per line; # comments and blank lines skipped.
# Shrink this file by purging the grandfathered row, never by widening the wall.
#
# ADD seed snapshots — tracked deliberately under .claude/.add/
.claude/.add/
