{
  "name": "koru-lsp",
  "version": "0.0.0",
  "private": true,
  "description": "Language Server Protocol wrapper for koruc --ccp",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "koru-lsp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "start": "node dist/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12",
    "vscode-uri": "^3.0.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
