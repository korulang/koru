diff --git a/koru_std/interpreter.kz b/koru_std/interpreter.kz
index 18cf5d27..3442f913 100644
--- a/koru_std/interpreter.kz
+++ b/koru_std/interpreter.kz
@@ -1842,9 +1842,8 @@ pub fn dischargeAllHandles(
     var progressed = true;
     while (progressed) {
         progressed = false;
-        var i = pool.handles.items.len;
-        while (i > 0) {
-            i -= 1;
+        var i: usize = 0;
+        while (i < pool.handles.items.len) : (i += 1) {
             const handle = pool.handles.items[i];
             if (handle.discharged) continue;
             if (pool.hasUndischargedDependents(handle.id)) continue;
