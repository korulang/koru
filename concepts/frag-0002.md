---
type: belief
id: frag-0002
provenance: session 2026-07-22 (Lars + Claude) — VAXIS PICKUP, `component` synthesizes a callable koru tor
ts: 2026-07-22
tags: [koru, transforms, synthesis, dead-strip, module-qualifier, canonicalize, pipeline-stages, dsl]
---

A koru library transform can **synthesize a whole callable tor** — tor decl
+ implementation — from a declaration, the way `store` synthesizes its
insert/query family. `vaxis:component(greet){ <text>{{ name:s }}</text> }` proves
it: the `{{ name:s }}` placeholders become the tor's typed input shape, and the
markup becomes an impl flow that composes the existing `write-at`. `greet` is then
a normal koru tor — callable, branchable, loopable. This extends [[frag-0001]]
(transforms lower to real invocations) up one level: transforms can also DEFINE
the tors those invocations call.

Making it actually reach the binary required three things, each a real gotcha:

1. **Return `whole_program`, not per-site `.appended`.** The shape checker builds
   its tor set by re-scanning `program.items`, so the synthesized tor must BE
   in the program it scans. A whole-program walk (find every declaration,
   synthesize, consume the declarations, hand back the rewritten program) is the
   clean shape — a library transform holds the whole AST, so it can. Per-site
   `.appended` did not make the tor visible.

2. **Run the collector in the `pre` transform stage.** Handler generation for a
   tor's impl runs after `pre` but the synthesized tor must exist before it —
   `[comptime|transform|pre]`.

3. **THE KILLER: stamp the consumer's module qualifier onto the synthesized
   tor's path, or `dead_strip` prunes it.** dead_strip keeps a tor only if
   its path-key appears in the invoked-set, keyed by `pathToString` (`module:seg`
   when a qualifier is present, bare `seg` otherwise). A normal tor is stamped
   with its module by the `canonicalize_names` pass, and the bare call site is
   stamped to match — so both key as `<module>:name`. A SYNTHESIZED tor is added
   AFTER canonicalize, so if its path carries a `null` qualifier it keys as bare
   `name`, the call keys as `<module>:name`, they don't match, and dead_strip
   silently strips the tor right before emit. The fix: build the tor (and its
   `impl_of`) path with `module_qualifier = <consumer module>` so it keys the same
   as the canonicalized call. General rule: **anything synthesized after
   canonicalize must self-apply what canonicalize would have done** — module
   qualifiers first among them.

   **Post-transform weld extends this (2026-09):** transform-appended imports
   merge *after* the first canonicalize pass. Welded modules parse with bare or
   basename qualifiers while callers use dotted logical names
   (`koru.signal_rt.breath:init`). Fix is two-layer: re-run `canonicalize_names`
   in `post_transform_weld` after merge, and teach `dead_strip.isPathUsed` to
   also match `{module_decl.logical_name}:{segments}` when the direct key misses
   (covers stale basename qualifiers canonicalize skips). Pin: `904_post_weld_dead_strip`.

Diagnosis lever that cracked #3: `KORU_DUMP_AST=1` dumps item counts per pipeline
stage. Seeing `post-transform items=22` → `emit-zig items=20` localized the loss to
the dead_strip pass between them, in one run — far faster than reading emitter code.
(Evidence: koru-libs `vaxis/index.kz` `component` proc + the `component synthesizes
a callable event` commit — referenced, not restated.)

Open: typed/formatted props (`:d`,`:b`) still need the fmt rail (only `:s` string
passthrough is zero-alloc today); `<child/>` nesting = a component-tor call the
parser must emit; the reactive loop (store change → re-render) is unbuilt.
