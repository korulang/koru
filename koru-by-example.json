{
  "_comment": "Curation config for the by-example artifacts. Corpus = union(passing tests under `categories`) ∪ `includedTests` − `excludedTests`. NO PROSE: per-test prose, category prose, intro, and rules have all been removed — the tests are the only source of truth (they cannot lie; prose drifts and contaminates). Generators emit verbatim passing tests + routing frontmatter only.",
  "_topics_comment": "`topics` drives generate-skills.js. Each topic emits TWO artifacts: (1) docs/by-example/<name>.md — the verbatim reference shard (every passing test under `clusters` ∪ `includedTests` − `excludedTests`); (2) skills/<name>/SKILL.md — a routing stub: frontmatter `description` (when the skill fires) + a pointer at the shard. No authored prose — the shard (verbatim tests) is the authority.",
  "topics": [
    {
      "name": "koru",
      "title": "Koru — core language",
      "description": "The core Koru language: events with named outcome branches, flows that dispatch (`|`) and chain (`|>`), subflows vs host procs, the `~` parser-mode rule, plus loops (label + jump) and literals/punning. Use when writing or reading any `.kz`/`.k` file and you need the foundational shapes. Start here before the narrower topic skills.",
      "clusters": [
        "000_CORE_LANGUAGE",
        "100_PARSER",
        "200_SYNTAX"
      ],
      "includedTests": [],
      "excludedTests": []
    },
    {
      "name": "koru-templates",
      "title": "Koru — control-flow templates (`for` / `if`)",
      "description": "Control flow in Koru: `~if` and `~for` as templates over continuation-return, not host keywords. Use when emitting conditionals or iteration in Koru, or when working on the template/lowering layer (`koru_std/control.kz`). Covers how `for`/`if` select continuations and lower per target.",
      "clusters": [
        "320_CONTROL_FLOW"
      ],
      "includedTests": [],
      "excludedTests": []
    },
    {
      "name": "koru-metaprogramming",
      "title": "Koru — metaprogramming (event globbing)",
      "description": "Compile-time metaprogramming in Koru: event globbing and compile-time event matching — the \"you are the compiler\" surface where userland code sits inside the pipeline. Use when writing compile-time event selection or pattern-matching over events. SEED TOPIC — currently scoped to event globbing; widen `clusters` in koru-by-example.json as the metaprogramming surface grows.",
      "clusters": [
        "700_EVENT_GLOBBING"
      ],
      "includedTests": [],
      "excludedTests": []
    }
  ],
  "categories": [],
  "includedTests": [
    "010_000_hello_world_koru",
    "010_001_hello_world",
    "010_002_simple_event",
    "020_001_simple_flow",
    "020_004_void_event",
    "020_005_void_event_chained",
    "020_014_pure_subflow_impl",
    "030_010_array_literal_simple",
    "030_012_struct_literal_inline",
    "201_multiple_branches",
    "209_branching_flow",
    "233_empty_payload_branches",
    "240_subflow_defines_semantics",
    "208_simple_server_flow",
    "310_001_import_registers_taps",
    "310_005_annotation_inline_syntax",
    "330_005_cleanup_obligation_satisfied",
    "330_006_cleanup_consumed_by_disposal",
    "400_060_fmt_blk_autodispose",
    "400_070_effect_branch_minimal",
    "610_001_string_basic",
    "620_001_fmt_ln_basic"
  ],
  "excludedTests": [],
  "tutorial": {
    "title": "Koru in a Page",
    "disclaimer": "This tutorial is prose synthesis on top of the test suite. It may contain errors or drift. The compiler's actual behavior — verified by tests in `tests/regression/` and source in `src/` — is the source of truth. When you find this document saying one thing and the compiler doing another, the conflict itself is the finding: flag it, don't paper over it.\n\n**Koru is greenfield.** There are no legacy users, no deprecated forms to support, no migration windows that constitute a contract. When the language changes, the change lands and tests/docs get rewritten in the same motion. If the parser temporarily accepts an older form while migration is in progress, that's internal scaffolding — not a feature. Write code in the current canonical form. Don't hedge for legacy.",
    "tests": [
      "010_001_hello_world",
      "010_000_hello_world_koru",
      "020_014_pure_subflow_impl",
      "201_multiple_branches",
      "240_subflow_defines_semantics",
      "330_005_cleanup_obligation_satisfied",
      "400_070_effect_branch_minimal"
    ]
  }
}
